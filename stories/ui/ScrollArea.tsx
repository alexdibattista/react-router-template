import React from "react";
import {
	ScrollArea as UIScrollArea,
	ScrollBar as UIScrollBar,
} from "../../app/components/ui/scroll-area";

export const ScrollArea = UIScrollArea;
export const ScrollBar = UIScrollBar;
